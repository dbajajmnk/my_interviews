# LangChain — 60-Second Recall

## Formula

```text
LangChain Agent = Model + Tools + Middleware + State + Store + Runtime Context + Structured Output
```

## 60-Second Answer

> LangChain provides a high-level agent-development layer. I use it for model/tool integration, middleware, structured outputs, runtime context, short-term state through checkpointers, long-term stores, retrieval, and streaming. Underneath, current agent execution is strongly connected to LangGraph-style durable runtime concepts.

## Recall Points

- High-level developer framework.
- Middleware matters.
- Short-term state != long-term store.
- LangGraph handles lower-level workflow control.
