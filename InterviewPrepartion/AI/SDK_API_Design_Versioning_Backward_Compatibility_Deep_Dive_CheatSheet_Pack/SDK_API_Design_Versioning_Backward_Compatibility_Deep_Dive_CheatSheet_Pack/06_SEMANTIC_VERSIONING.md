# Semantic Versioning

## SemVer

```text
MAJOR.MINOR.PATCH
```

## Meaning

- PATCH = backward-compatible fixes
- MINOR = backward-compatible features
- MAJOR = breaking changes

## Rule

Semantic versioning is only useful when backed by an explicit compatibility policy.
