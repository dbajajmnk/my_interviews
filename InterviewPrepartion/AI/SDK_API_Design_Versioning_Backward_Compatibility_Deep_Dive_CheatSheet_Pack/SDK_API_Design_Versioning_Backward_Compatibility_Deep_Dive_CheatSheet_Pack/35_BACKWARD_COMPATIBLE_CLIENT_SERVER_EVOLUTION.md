# Backward-Compatible Client/Server Evolution

## Example

```text
Old client + new server  → should work
New client + old server  → feature detect / degrade
```

## Rule

Design both upgrade directions deliberately.
